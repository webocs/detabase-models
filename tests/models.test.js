const chai = require('chai');
const {detabaseModels} = require('../index');

chai.should();

describe('Detabase models unit test', () => {
  after(async () => {});

  it(`Create model`, async () => {});
});
